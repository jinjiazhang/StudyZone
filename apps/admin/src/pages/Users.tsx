/**
 * Placeholder — a real Admin API endpoint (e.g. GET /api/v1/admin/users)
 * would replace this. Listed here so the IA is in place.
 */
export function Users() {
  return (
    <div>
      <h1 style={{ fontSize: 24, marginBottom: 16 }}>用户管理</h1>
      <p style={{ color: '#6b7280' }}>
        用户列表、风控、客服工单将在此模块迭代。当前 MVP 未暴露后端 admin
        端点，仅占位。
      </p>
    </div>
  );
}
