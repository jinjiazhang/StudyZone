interface:
  display_name: "Generate Lesson Data"
  short_description: "Create lesson data from textbook page images."
  default_prompt: "Use $generate-lesson-data to visually inspect StudyZone textbook page images, create or regenerate textbook-aligned lesson-data, then validate schema, answer correctness, uniqueness, and answer-order randomness."
